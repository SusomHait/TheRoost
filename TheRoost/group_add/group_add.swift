import SwiftUI

struct GroupAdd : View {
    var body: some View {
        VStack {

        }
        .padding()
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(.backing)
    }
}

#Preview {
    Dashboard()
}
